fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
pyyaml>=6.0.2
pydantic>=2.9.0
aiosqlite>=0.20.0
python-multipart>=0.0.12
# GPL-3.0. This is why Mesh-Spy as a whole is GPL-3.0; see the README.
meshtastic>=2.7.10
# MIT.
meshcore>=2.3.9
# Arrives via meshtastic anyway, but app/core/ports.py imports it directly to
# enumerate serial ports the same way on Windows, Linux and macOS, so it is
# declared rather than relied on as someone else's transitive dependency.
pyserial>=3.5
